# Commands to start on workspace startup
tasks:
  - init: apt update && apt-get -yqq install gettext-base
# Ports to expose on workspace startup
ports:
  - port: 80
    onOpen: open-browser
    visibility: public